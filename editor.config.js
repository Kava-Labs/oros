/**
 * To setup prettier and have it auto format with VS Code,
 * follow the below steps:
 *
 * # Install the Prettier Extension in VS Code
 *   1) CMD + Shift + P, then type and choose "Extensions: Install Extensions"
 *      - can also use extensions from the side toolbar
 *   2) Search for: "Prettier - Code formatter" and install
 * # Configure the VS Code Editor settings to use prettier
 *   and auto format on file save
 *   1) CMD + Shift + P, then type and choose "Preferences: Open Workspace Settings (JSON)"
 *   2) Copy the JSON object below and paste it into your settings (or add just the new
 *      key/values to your existing setup). Save the file
 *   3) (optional) - You may need to reload the VS Code workspace for this to take effect.
 *      CMD + Shift + P, then type: "Reload Window" and hit enter
 */

/*
// settings.json

{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "[typescript]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  },
  "[javascript]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  }
}

*/
